#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    vector<int> v(t);

    for (int i = 0; i < v.size(); i++)
    {
        cin >> v[i];
    }

    

    int sereja = 0, dima = 0;
    // for (int i = 0; i < v.size(); i++)
    // {
    //     if(i%2 == 0) sereja+=v[i];
    //     else dima += v[i];
    // }
    

    for (int i = 0; i < v.size(); i++)
    {
        
    }
    
    cout << sereja << " " << dima << endl;
    
    return 0;
}