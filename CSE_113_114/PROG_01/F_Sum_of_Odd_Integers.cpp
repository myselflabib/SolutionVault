#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        long long n, k;
        cin >> n >> k;

        if (n >= k * k && n % 2 == k % 2) {
            cout << "YES" << endl;
        } 
        
        else {
            cout << "NO" << endl;
        }
    }

    return 0;
}

/*
একজনকে এই কোডটা বুঝাতে গেলে Md., তুমি ধাপে ধাপে সহজ ভাষায় ব্যাখ্যা করতে পারো। নিচে আমি একটা সুন্দরভাবে সাজানো ব্যাখ্যার ধরন দিলাম, যেটা তুমি ব্যবহার করতে পারো যখন কাউকে বোঝাবে:

---

### 🧩 Step-by-Step বোঝানোর কৌশল

#### 🔹 ১. সমস্যা টা আগে বুঝিয়ে দাও
> “আমরা দেখতে চাই `n` সংখ্যাটাকে `k`-টি ভিন্ন বিজোড় সংখ্যা দিয়ে যোগ করে বানানো যাবে কি না। যেমন, `n = 16` আর `k = 4` হলে — `1 + 3 + 5 + 7 = 16` → এটা সম্ভব।”

#### 🔹 ২. Minimum possible যোগফল বোঝাও
> “যদি `k`-টি ভিন্ন বিজোড় সংখ্যা নেই, তাহলে সবচেয়ে ছোট যোগফল হবে `1 + 3 + 5 + ... + (2k - 1)`  
> এইটার shortcut formula হলো `k²`।”

📌 উদাহরণ:  
`k = 3` → `1 + 3 + 5 = 9` → তাই `n` এর মান কমপক্ষে `9` হতে হবে।

#### 🔹 ৩. Parity (even/odd) ব্যাখ্যা করো
> “যেহেতু বিজোড় সংখ্যা যোগ করলে parity নির্ভর করে `k` এর উপর:
- `k` জোড় হলে → যোগফল জোড়
- `k` বিজোড় হলে → যোগফল বিজোড়  
তাই `n` এবং `k` এর parity একই হতে হবে।”

📌 উদাহরণ:  
`k = 3` → বিজোড় → `n` ও বিজোড় হতে হবে  
`k = 4` → জোড় → `n` ও জোড় হতে হবে

#### 🔹 ৪. Code-এর মূল শর্তটা দেখাও
```cpp
if (n >= k * k && n % 2 == k % 2)
```
> “এই line টা check করে:
- `n` কি `k²` এর চেয়ে বড় বা সমান?
- `n` এবং `k` এর parity কি একই?

যদি দুইটা শর্তই ঠিক থাকে → `YES`  
না হলে → `NO`”

---

### 🎯 Bonus Tips

- উদাহরণ দিয়ে বোঝাও: “ধরো `n = 10`, `k = 3` → `k² = 9`, কিন্তু `n` জোড়, `k` বিজোড় → mismatch → Output: NO”
- প্রশ্ন করো: “তুমি কি বুঝতে পারছো কেন `k²` লাগে?”
- চাইলে তুমি একটা ছোট quiz বানিয়ে দিতে পারো শেখার জন্য

---

তুমি চাইলে আমি তোমার জন্য একটা Bengali-friendly version বানিয়ে দিতে পারি, যেটা তুমি অন্যদের শেখাতে ব্যবহার করতে পারো। বলো, দরকার হলে বানিয়ে দিই! 😊
*/
